import React from 'react';

// This component is currently unused but is given a valid structure 
// to prevent build errors caused by empty .tsx files.
export const Pagination: React.FC = () => {
  return null;
};
